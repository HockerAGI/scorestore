{
  "name": "scorestore",
  "version": "1.1.0",
  "private": true,
  "description": "SCORE Store — Static + Netlify Functions (Stripe + Envia).",
  "engines": {
    "node": ">=18 <21"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo \"static site\"",
    "lint": "node -c netlify/functions/create_checkout.js && node -c netlify/functions/quote_shipping.js && node -c netlify/functions/stripe_webhook.js && node -c netlify/functions/envia_webhook.js"
  },
  "dependencies": {
    "stripe": "^16.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.43.0"
  }
}