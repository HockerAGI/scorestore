[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# (Opcional) si quieres forzar data en root
# [[redirects]]
#   from = "/data/*"
#   to = "/:splat"
#   status = 200